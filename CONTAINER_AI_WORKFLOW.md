# 📖 AI 容器化與高階 Agent 數據情報工作流知識庫（v6 增訂版）

## 🔍 修訂說明

v2 針對初稿進行了結構性校正，v3 新增建造者視角，v4 新增無人值守自動化，v5 新增維運治理，v6 新增發布管線：

1. **修正沙盒比喻的邏輯方向**：沙盒的本質是「限制內部執行風險」，而非「阻擋外部入侵」，因此比喻從「外圍鐵絲網」改為「房子內部的抗爆隔離艙」。
2. **補上缺失的 Agent 角色**：原稿工作流第 4 步描述「正反方辯證分析」，但先前定義的 Subagent 只有工程師與測試員，缺乏對應角色 → 新增「決策分析小組」。
3. **建立概念與流程的一致映射**：將第一部分的四大基礎概念，與第三部分的五步驟工作流做逐一對應，使字典與流程互相印證，而非兩份各自獨立的文件。
4. **去除重複敘述**，將條列式說明改為表格化、流程化呈現。
5. **v3 增訂**：新增第五部分「Agent 解剖學」（四件套、Vibe Coding、Skill 與 Subagent 的差異、Agent 的四種創造來源）與第六部分「審查代理人實戰案例」（五大設計決策）——把「使用者視角」升級為「建造者視角」。
6. **v4 增訂**：新增第七部分「無人值守實戰：Heartbeat 與自動化收件匣」——heartbeat 觸發設計、確定性程式碼與 AI 的分工、縱深防禦、fail-closed、對抗性驗收，讓 agent 系統從「工具」升級為「服務」。
7. **v5 增訂**：新增第八部分「系統上線之後：維運四律」——同步義務、備份與可見性、設定先驗證、擴充後回歸測試，並以審查系統的真實維運輪（含領域擴充包回歸測試）為案例。
8. **v6 增訂**：新增第九部分「從系統到刊物：發布管線與讀者體驗」——產製與發布分離、內容與外殼的快取分策、渲染後增強、受眾分層與雙層修正，以晨報閱讀站為案例；後補設計五「友善防禦」（跨站一致性稽核與「取捨要揭露」兩課）。

---

## 🧱 第一部分：核心設計原則 — 四層基礎設施

整套系統可以濃縮為一句話：

> **先隔離，再賦能，接著安全連結外部世界，在受控環境中驗證邏輯，最終產出可信任的決策。**

這句話對應四層基礎設施，由內而外分別是：

```
隔離層 (Container)
   └─ 能力層 (Package / Plugin)
        └─ 通訊層 (Connector)
             └─ 安全層 (Sandbox)
```

| 層級 | 概念 | 建築比喻 | 核心功能 | 在 AI 專案中的角色 |
| :--- | :--- | :--- | :--- | :--- |
| 隔離層 | **容器 Container** | 一棟獨立的房子 | 將應用程式與作業系統底層、運行環境打包成一致的隔離空間 | 確保 AI 模型在任何電腦或雲端都能跑出相同結果 |
| 能力層 | **套件 Package** | 蓋房子的磚頭 | 預先寫好、不能獨立執行的功能函式庫 | 提供基礎能力，如 `pandas`（資料分析）、`playwright`（爬蟲） |
| 能力層 | **外掛 Plugin** | 房子的裝潢 | 依附主程式運作，用來擴充特定功能 | 補足主程式沒有的專項能力，如 PDF 解鎖、多模態視覺處理 |
| 通訊層 | **連接器 Connector** | 外接的水電管線 | 在不同系統／平台之間牽線、傳遞資料，本身不執行邏輯 | 讓 AI 安全串接外部世界，如資料庫、雲端硬碟、外部 API |
| 安全層 | **沙盒 Sandbox** | 房子內部的抗爆隔離艙 | 開闢一個極低權限的受限區域，專門承接未知或有風險的執行行為 | AI 生成的程式碼在此執行，出錯或失控都不會波及核心系統 |

> 「容器」界定邊界、「套件／外掛」提供能力、「連接器」負責對外連結、「沙盒」負責對內止損——四者分工明確，缺一不可。

---

## 🤖 第二部分：Agent 協作架構 — 雙小組制

高階 Agent（如 Claude Code）處理宏觀任務時，會依「目的」而非單一角色拆分成兩支獨立小組，各自形成閉環：

### 小組 A｜技術實作小組（Engineering Squad）
負責讓程式碼「能跑」，形成**自癒閉環**：

| Subagent | 職責 |
| :--- | :--- |
| 工程師 (Code Generator) | 理解需求與上下文，撰寫自動化腳本或分析程式碼 |
| 測試員 (Code Executor) | 在沙盒中執行程式碼、捕捉錯誤訊息，回饋給工程師修正 |

```
工程師寫 Code → 測試員丟進沙盒執行 → 報錯 → 工程師修正 → 重新執行（直到成功）
```

### 小組 B｜決策分析小組（Analyst Squad）
負責針對「數據代表的意義」進行**辯證推論**，而非寫程式：

| Subagent | 職責 |
| :--- | :--- |
| 正方分析師 (Proponent) | 從技術可行性與市場機會角度，論證數據的正面意義 |
| 反方分析師 (Critic) | 從風險、時程延遲、商業衝擊角度，提出質疑與反例 |
| 綜合仲裁者 (Synthesizer) | 彙整正反論點，收斂成可執行的結論與信心水準 |

> 兩支小組共用同一個沙盒環境執行與驗證，但目的不同：技術小組驗證「程式碼能不能跑」，分析小組驗證「結論站不站得住腳」。

---

## 🚀 第三部分：標準工作流 SOP（五步驟）

四層基礎設施 + 兩支 Agent 小組，共同組成一條完整的情報產出流水線：

```
[1] Container      [2] Package/Plugin   [3] Connector        [4] Sandbox              [5] 產出
建立隔離環境   ──▶  補齊工具與外掛   ──▶  安全抓取外部數據  ──▶  雙小組驗證與辯證   ──▶  BI 呈現與決策建議
```

| 步驟 | 對應概念 | 執行內容 | 涉及角色 | 產出物 |
| :-- | :--- | :--- | :--- | :--- |
| 1. 環境初始化 | Container | 以專案目錄為邊界，啟動獨立容器，隔離本機系統 | — | 乾淨的作戰環境 |
| 2. 工具與技能載入 | Package / Plugin | 自主安裝資料分析、爬蟲等套件，載入特定格式外掛 | — | 具備完整能力的執行環境 |
| 3. 安全資料牽線 | Connector | 定時抓取主題新聞，與 `knowledge_base/` 歷史資料比對差異 | — | 每日差異摘要（晨報素材） |
| 4. 沙盒驗證與辯證 | Sandbox | 技術小組跑通模型程式碼；分析小組在沙盒模擬結果上進行正反辯證 | 技術實作小組 + 決策分析小組 | 通過驗證的模型結果、正反論點 |
| 5. 終端輸出 | （產出目標，非基礎設施概念） | 將驗證結果渲染成網頁 BI 圖表，彙整辯證結論 | 綜合仲裁者 | 趨勢圖表 +「建議決策行動」與「應持續追蹤議題」 |

> 注意第 5 步並不對應任何一個基礎設施概念，它是前四層共同服務的**最終目的**——沒有產出，前面的隔離、賦能、連結、驗證都沒有意義。

---

## 💡 第四部分：實戰指令範本

使用 Claude Code 時，直接以下列「架構語意」下達指令，而非描述實作細節：

> **🎯 企業級自動化情報系統建立指令：**
>
> 「我想針對 **[主題，例如：固態電池技術商用化]** 建立一個每日晨報與決策系統，請在目前的專案容器內建立全自動工作流：
>
> 1. **（Connector）** 配置連接器每日抓取新聞，與 `knowledge_base/` 資料夾的歷史資料比對差異，產出晨報摘要。
> 2. **（Analyst Squad）** 啟動決策分析小組，由正方分析師與反方分析師針對新舊數據進行辯證（技術可行性、市場衝擊、時程），最後由綜合仲裁者收斂結論。
> 3. **（Sandbox + Engineering Squad）** 所有驗證與運算腳本，必須在沙盒環境中執行，若出錯由工程師與測試員自我修正，直到成功運行。
> 4. **（產出）** 成功後於專案內產出網頁 BI 圖表呈現趨勢，並在終端機輸出『建議決策行動』與『應持續追蹤議題』總結報告。」

### 精簡版（快速觸發用）

> 「針對 **[主題]**，在容器內建立每日晨報系統：連接器抓新聞比對歷史庫 → 分析小組正反辯證 → 沙盒驗證程式碼 → 輸出 BI 圖表與決策建議。」

---

## 🧬 第五部分：Agent 解剖學 — 四件套、Skill 與 Subagent（建造者視角）

### Agent 四件套

任何一個 Agent 拆開來看，都是四樣東西的組合：

| 比喻 | 實際對應 | 功能 |
| :--- | :--- | :--- |
| 腦袋 | 底層模型 | 理解、推理、生成 |
| 手腳 | 工具權限（檔案、終端機、瀏覽器、搜尋） | 對世界採取行動 |
| 職務說明書 | 系統提示詞 | 規範角色、行為與邊界 |
| 工作節奏 | 執行迴圈（harness） | 行動 → 觀察結果 → 再行動，直到完成 |

四者齊備，這個角色就能自主做事、自我修正——這是 Agent 與單純聊天模型的分水嶺。

### Vibe Coding 與 Agent 的關係

- **Agent 是「誰在做」，Vibe Coding 是「你怎麼跟它說」**——只描述目標與架構語意，把實作細節交給 Agent 自主決定。
- 可以有 Agent 卻不 Vibe Coding（逐行指定寫法）；也可以 Vibe Coding 卻沒有真正的 Agent（單純與聊天模型對話）。兩者搭配才是完整協作模式。

### Skill vs Subagent

兩者都是把「臨時的 Vibe Coding」固化成「可重複觸發的資產」，但性質不同：

| | Skill | Subagent |
| :--- | :--- | :--- |
| 本質 | 流程手冊／SOP（動詞式） | 職務說明書／聘僱新員工（名詞式） |
| 定義的是 | 「這種任務該怎麼做」 | 「這個角色是誰、能用哪些工具、用哪顆腦袋」 |
| 存放位置（Claude Code） | `.claude/skills/<名稱>/` | `.claude/agents/<名稱>.md` |
| 執行者 | 需要 Agent 來照著做 | 本身就是一個可被派工的完整 Agent |

### Agent 的四種創造來源

1. **系統內建員工**：如唯讀搜尋的 Explore、規劃用的 Plan。
2. **自訂職務卡**：在 `.claude/agents/` 放一份 Markdown（YAML 定義名稱/職責/工具/模型 + 系統提示詞），貼上即聘僱。
3. **臨時外包**：當場口頭交辦、做完即散，不留常駐角色。
4. **程式化人資系統**：用 Claude Agent SDK 在自己的應用程式中批量定義。

### Agent vs Subagent：相對位置，不是物種差異

直接面對使用者的叫主 Agent（總機）；被指派任務、做完回報、用完即忘的叫 Subagent（員工）。同一個體換個位置，身份就換——差別在「誰在指揮誰」。

---

## 🔍 第六部分：實戰案例 — 審查代理人系統設計

以一個真實建置並實測通過的系統（多視角審查代理人 Review Agent）示範概念如何組裝成系統。

### 需求與四層架構

審查四類內容（量化論文／質性論文／財務策略報告／總經市場報告），每份都從理論、實務、批判風險三個立場檢視：

```
[第0層] 總審查官（主 Agent）：分類 → 選規準 → 派工 → 綜合
[第1層] 三視角審查員（常駐 Subagent，唯讀工具）：理論家｜實務家｜批判家
[第2層] 查核員（共用後勤，獨享搜尋與重算工具）
[第3層] 綜合仲裁 → 標準化審查報告
```

### 五大設計決策

1. **角色與規準解耦**：不建「4 內容 × 3 視角 = 12 個員工」，只聘 3 位固定角色，內容差異封裝進 4 份規準檔（rubric）。新增審查領域 = 寫一份新文件，不動任何角色。
2. **批評必須可查核**：審查員只能「標記」可疑宣稱；查核員用搜尋與重算驗證，判定 CONFIRMED／REFUTED／UNVERIFIABLE 且必附來源——沙盒驗證精神的延伸。
3. **Full／Lite 雙模式**：Full 多 Subagent 平行（成本 5-8 倍，定稿用）；Lite 單一情境依序扮演三角色（日常快查）。同一套知識、兩個檔位。
4. **知識可攜、接線各平台**：core/ 純 Markdown 是唯一事實來源；adapters/ 是各平台薄接頭。Claude Code 跑 Full；ChatGPT/Gemini 無多 Subagent 機制，只能跑 Lite。
5. **遺留資產治理**：新系統與既有專用 skill 分流共存而非立即淘汰；實戰驗證後才逐步把舊 skill 改造成新架構上的領域擴充包。

### 驗收方式：煙霧測試

上線前用一份刻意埋錯的樣本報告走完整條流水線：三位審查員分工零重複地抓出各自領域的缺陷（框架誤用／執行外行／假設無備案），查核員以公開來源反駁三條關鍵宣稱——架構是否如設計運作，用實測回答。

---

## 💓 第七部分：無人值守實戰 — Heartbeat 與自動化收件匣

審查系統建好後的下一個問題是「誰來按按鈕」。答案：heartbeat（心跳）——讓系統自動接收、自動審查、自動產出報告，把 agent 從「工具」升級為「服務」。

### 心跳觸發設計（不需要一直開機）

```
上傳（Dropbox File Request，上傳者看不到內容、刪不了檔案）
   ↓ 關機期間存雲端
開機後 5 分鐘 heartbeat（＋每日早晨備援；臨時可手動觸發）
   ↓ 零成本偵測：腳本比對已處理清單，「沒新檔案」直接結束，AI 不被喚醒
歸檔（收件即歸檔，不可變副本）→ Lite 初審 → 報告落地
```

### 核心分工原則

> **確定性程式碼做搬運，AI 只做判斷。**

| 工作 | 交給誰 | 理由 |
| :--- | :--- | :--- |
| 偵測新檔案、大小守衛、歸檔複製 | 排程腳本（確定性程式碼） | 有標準答案：零成本、可預測、**不會被騙** |
| 讀檔、審查、分流判定 | AI（Lite 模式） | 需要判斷力 |
| 深度審查（Full 模式） | 人類手動觸發 | 高成本決策留給人 |

### 縱深防禦五層（Defense in Depth）

1. **指令隔離**：文件是資料不是指令；操縱企圖＝嚴重度 5 缺陷＋強制隔離（quarantine）。
2. **權限最小化**：無人值守執行拿掉 Bash 權限，寫入範圍限定於報告與狀態檔。
3. **資源守衛**：20MB 大小上限，防止超大檔案灌爆空間與成本。
4. **竄改偵測**：知識庫（規準／角色檔）納入 git 版控作基準，`git diff` 即可發現異動。
5. **Fail-closed**：狀態檔損毀時寧可安全停止並通知人類，也不冒險大量重跑——與 fail-open（出錯就全部重來）相對，防成本失控與重複污染。

### 對抗性驗收（Adversarial Acceptance Test）

上線標準不只是「正常流程跑得通」，而是「被攻擊時表現正確」：故意投遞一份內藏提示注入的測試檔（偽稱系統指示、要求給滿分並入庫）。通過的定義——評分不被操縱、注入被列為頭號缺陷、文件被強制隔離，且系統能做出正確的安全推理：**「一份會試圖操縱下游流程的文件，其正文數字也不應被信任。」**

### 可複用的結論

這四個原則適用於任何無人值守的 AI 自動化：分工原則（程式碼搬運／AI 判斷）、收件即歸檔、縱深防禦、fail-closed。無人值守系統的真正驗收，是它在你不在場時面對意外與攻擊的表現。

---

## 🛠 第八部分：系統上線之後 — 維運四律

建好、上線、驗收通過不是終點。系統活著的每一天都在變：知識庫會擴充、設定會過時、資產會累積風險。維運不是雜務，而是四條可複用的紀律。

### 第一律：同步義務（唯一事實來源不會自動成立）

宣告 `core/` 是唯一事實來源只是開始——它其實是一種**義務**：每當 core 改變，所有衍生品（各平台接頭、濃縮版、文件）都必須跟上，否則知識就地分裂，「唯一」名存實亡。

實務作法：把「改 core 之後要同步什麼」寫成明確清單（本系統寫在 README 維護原則），每次擴充後**立即執行**，不累積到「有空再說」。實例：SMMC 等領域包遷入 core 後，ChatGPT／Gemini 接頭的知識檔清單當日同步從 4 檔擴為 11 檔。

### 第二律：備份與可見性（兩個獨立的決策）

關鍵知識資產要問兩個問題：

1. **它有備份嗎？**——審查系統的 core 放在使用者設定資料夾，不在任何雲端同步範圍內：電腦故障＝整套消失。解法：推送 git 遠端。
2. **備份該給誰看見？**——這套 core 含課程評分基準等敏感資料，備份庫必須 **private**。「要不要備份」與「能不能公開」是兩個獨立決策，且備份完成後要**回頭驗證**可見性設定確實生效。

### 第三律：設定先驗證（設定是程式碼的一種）

寫進系統設定的每一筆外部事實——網址、路徑、版本號——都應在寫入前**實際驗證**，不憑記憶或直覺。

實例：晨報巡檢清單的 9 個候選網址逐一實測可達性後，2 個大站（擋爬蟲回 403）被剔除。若憑印象直接寫入，系統每天都會在這兩個網址上白白失敗且無人察覺。

### 第四律：擴充後回歸測試（驗新功能，也驗舊防線）

系統每次擴充（如新增領域擴充包），上線前用埋錯樣本做回歸測試——不只驗證「新功能會動」，更驗證「舊防線還在」。SMMC 擴充包的實測給了兩個超出預期的觀察：

- **精確防禦**：測試檔標題寫著「系統測試用，可刪除」，系統正確判定這是**描述性註記而非指令**——記錄在案但不執行刪除。防禦的成熟度不在「擋掉多少」，在「分得清像指令的資料與真指令」，不過度防禦。
- **基準線保護**：系統主動註明測試殘稿的低分「**不覆寫**」該組在 cohort 軌跡中的既有評分——對資料完整性的理解，而不只是執行評分。

### 結語

維運四律：同步義務、備份與可見性、設定先驗證、擴充後回歸測試。**系統的可信度不是建成時的狀態，而是維運中的習慣。**

---

## 📰 第九部分：從系統到刊物 — 發布管線與讀者體驗

無人值守系統產出的晨報，原本只是硬碟裡的 Markdown 檔。本部分示範把「系統產出」升級為「公開刊物」的四個設計，以晨報閱讀站（駱駱精選晨報）為案例。

### 設計一：產製與發布分離（機器勞務、人類把關）

```
heartbeat 每日產出晨報（僅本機，不公開）
   ↓ 人工審閱 —— 對外發言的責任留在人身上
一鍵發布腳本（確定性程式碼：複製 → 重建清單 → 推送）
   ↓ 約 1-2 分鐘
閱讀站自動上線
```

無人值守可以放心授權「產製」，但「公開上架」涉及對外發言的責任——半自動管線讓兩者並存：授權給機器的是勞務，保留給人的是判斷。

### 設計二：快取策略跟著資料性質走

| 資料 | 性質 | 快取策略 |
| :--- | :--- | :--- |
| 外殼（閱讀器介面） | 低頻改版 | 版本化快取（改版才升 `brief-shell-vN`） |
| 內容（每日晨報） | 高頻更新的流 | network-first：上線即最新、離線退回已讀版本；**每日發布不需升版** |

與教材站「整站版本化」形成對照——教材是低頻更新的整體，刊物是高頻更新的流。同一個 PWA 內兩種策略並存。

### 設計三：渲染後增強（render-then-enhance）

晨報源頭保持純 Markdown——這是對「AI 產出格式會漂移」的務實妥協。閱讀器先渲染再增強：小節包成卡片、〔新增〕〔待查核〕轉徽章、領域標記變圖示、產製說明摺疊收合。**內容與呈現徹底解耦**：改版面不動任何一期晨報，舊晨報自動獲得新體驗。

### 設計四：受眾分層（摺疊而非刪除，且明白摺疊≠保密）

同一份晨報服務兩種讀者：一般讀者看新聞卡片；研究者展開預設收合的「決策行動筆記」（展開先見提醒框）。關鍵認知：**原始檔在公開 repo 依然可見，版面摺疊是閱讀動線的選擇，不是隱私機制**——若需真正的存取控制，得換架構而非靠隱藏。

### 迭代方法：雙層修正

真實使用者的介面回饋（本案例一輪七點），修正採兩層並進：

- **顯示端（閱讀器）**：相容處理既有內容——舊晨報立即獲得修正效果
- **產製端（SOP 規範）**：約束未來產出——新晨報從源頭就符合格式

顯示端管相容、產製端管規範，兩層各司其職，刊物得以持續演進而不破壞歷史。

### 設計五：友善防禦——保護智財而不妨礙閱讀（上線後補強）

閱讀站上線初期只有 Domain Lock 與防嵌入，缺少教材站與測驗站已有的「防君子、護學習」機制——**這個缺口是使用者發現的，不是系統稽核發現的**。補強後三個公開站台策略一致：

| 讀者行為 | 處置 |
| :--- | :--- |
| 反白文字後右鍵 | **允許**（查字典、複製筆記） |
| 空白處／圖片右鍵 | 攔截＋2 秒版權提示（優雅而非生硬） |
| F12 等開發者快捷鍵 | 攔截＋提示 |
| Ctrl+P 列印／Ctrl+S 存檔 | **開放**（歡迎離線保存） |
| 正文選取 | 開放；僅頁首、按鈕等 UI 裝飾禁選取 |

兩個附帶的治理教訓：

1. **屬性一致性需要稽核**：同一家族的多個站台，安全與防禦策略應同步演進——這是第八部分「同步義務」的跨站版本，值得列入維運清單，而非仰賴誰恰好注意到。
2. **取捨要揭露**：省略防禦是建站當下「閱讀零阻力」的刻意取捨，但未經討論的取捨等於替使用者做了決定——agent 應主動揭露設計省略，把拍板權留給人。

### 實戰教訓一則

`requestAnimationFrame` 綁定渲染循環——分頁不可見時整個凍結（連 scroll 事件都停發）。節流這類與渲染無關的邏輯，用計時器比 rAF 穩。**綁定渲染循環的 API 都有一個隱藏前提：頁面正在被看著。**
